<!doctype html>
<html lang="tr">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>deneme 1</title><!--probe-->
<script src="ek-ders.js" defer></script>
<meta name="description" content="YKS Kurs/Etüt merkezleri için birebir ders, istek havuzu ve öğrenci eksik takip otomasyonu. Tüm veriler bu bilgisayarda saklanır." />
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='22' fill='%2314b8a6'/><text x='50' y='68' font-size='52' text-anchor='middle' fill='white' font-family='Arial'>Y</text></svg>" />

<link rel="stylesheet" href="vendor/fonts.css">
<script src="vendor/tailwind.js"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        fontFamily: { sans: ["Inter", "system-ui", "-apple-system", "Segoe UI", "Roboto", "Arial", "sans-serif"] }
      }
    }
  };
</script>
<link rel="stylesheet" href="vendor/fontawesome.css">
<script src="vendor/chart.js"></script>
<script src="vendor/html2canvas.js"></script>

<style>
  html { scroll-behavior: smooth; }
  body { background:#f4f7f9; font-family: Inter, system-ui, sans-serif; -webkit-font-smoothing: antialiased; }
  ::-webkit-scrollbar { width: 8px; height: 8px; }
  ::-webkit-scrollbar-thumb { background:#cbd5e1; border-radius: 8px; }
  ::-webkit-scrollbar-track { background:transparent; }
  .kart { background:#fff; border:1px solid #e2e8f0; border-radius:1.25rem; box-shadow:0 1px 2px rgba(15,23,42,.05); }
  @keyframes belir { from{opacity:0; transform:translateY(6px);} to{opacity:1; transform:none;} }
  .belir { animation: belir .18s ease-out both; }
  @keyframes toastGir { from{opacity:0; transform:translateY(10px);} to{opacity:1; transform:none;} }
  .toast { animation: toastGir .2s ease-out both; }
  .hucreBtn { width:100%; height:2.15rem; border-radius:.55rem; transition:all .12s ease; }
  .hucreBtn:active { transform:scale(.94); }
  input[type=date], input[type=time] { cursor:pointer; }
  .print-goster { display:none; }
  @media print {
    body { background:#fff !important; }
    .no-print, #pngModal, #onayModal, #waModal, #toastAlan { display:none !important; }
    body * { visibility:hidden; }
    #yazdir, #yazdir * { visibility:visible; }
    #yazdir { position:absolute; left:0; top:0; width:100%; border:none; box-shadow:none; border-radius:0; }
    .print-goster { display:block !important; }
    * { -webkit-print-color-adjust:exact; print-color-adjust:exact; }
  }

    /* --- Havuz kartı → haftalık takvim sürükle-bırak --- */
    .dnd-bos { cursor: grab; }
    .dnd-bos:hover { background-color: #f0fdfa; box-shadow: inset 0 0 0 1.5px #99f6e4; }
    .dnd-uygun { background-color: #f0fdfa; box-shadow: inset 0 0 0 2px #2dd4bf; }
    .dnd-kilit { cursor: not-allowed; }
    .dnd-kilit:hover { background-color: #fef2f2; }
  </style>

  <!-- ============ ATLAMALI MENÜ STİLİ (eklendi — mevcut stile/HTML'e dokunmaz) ============ -->
  <style id="atlamaMenuStil">
    #atlamaMenu {
      position: sticky;
      top: var(--atlamaTop, 66px);
      z-index: 25;
      background: rgba(255, 255, 255, 0.94);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid #e2e8f0;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.05);
    }
    #atlamaMenuIcerik {
      display: flex;
      align-items: center;
      gap: 6px;
      max-width: 1440px;
      margin: 0 auto;
      padding: 5px 12px;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      overscroll-behavior-x: contain;
      scrollbar-width: none;
    }
    #atlamaMenuIcerik::-webkit-scrollbar { display: none; }
    .atlama-btn {
      flex: 0 0 auto;
      white-space: nowrap;
      -webkit-tap-highlight-color: transparent;
      cursor: pointer;
      border: 1px solid #e2e8f0;
      background: #ffffff;
      color: #475569;
      font-size: 12.5px;
      font-weight: 600;
      line-height: 1;
      padding: 6px 13px;
      border-radius: 999px;
      transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease, box-shadow 0.15s ease;
    }
    .atlama-btn:hover {
      background: #f1f5f9;
      border-color: #cbd5e1;
      color: #0f172a;
    }
    .atlama-btn.aktif {
      background: #14b8a6;
      border-color: #14b8a6;
      color: #ffffff;
      box-shadow: 0 1px 3px rgba(20, 184, 166, 0.35);
    }
    .atlama-btn:focus-visible {
      outline: 2px solid #14b8a6;
      outline-offset: 2px;
    }
    #ozetBolum, #analizBolum, #yonetimBolum, #havuzBolum, #planKart, #derslerBolum {
      scroll-margin-top: calc(var(--atlamaTop, 66px) + 50px);
    }
    @media print {
      #atlamaMenu { display: none; }
    }
  </style>
</head>

<body class="text-slate-800 antialiased">

<!-- ============ ÜST BAR ============ -->
<header class="no-print sticky top-0 z-30 bg-white/90 backdrop-blur border-b border-slate-200/80">
  <div class="max-w-[1440px] mx-auto px-4 md:px-6 h-16 flex items-center justify-between gap-3">
    <div class="flex items-center gap-3 min-w-0">
      <div class="w-10 h-10 rounded-2xl bg-teal-500 flex items-center justify-center text-white shadow-sm shrink-0">
        <i class="fa-solid fa-graduation-cap text-lg"></i>
      </div>
      <div class="min-w-0">
        <h1 class="font-bold text-slate-900 leading-tight truncate">YKS Birebir Takip</h1>
        <p class="text-[11px] text-slate-500 truncate">Birebir Ders, İstek ve Öğrenci Eksik Takip Otomasyonu</p>
      </div>
    </div>
    <div class="flex items-center gap-2">
      <span class="hidden md:inline-flex items-center gap-1.5 text-[11px] font-medium text-slate-400 bg-slate-100 rounded-full px-3 py-1.5">
        <i class="fa-solid fa-shield-halved text-teal-500"></i> Veriler bu cihazda saklanır
      </span>
      <button onclick="indirApp()" title="Bu programın dosyasını (index.html) bilgisayarına indirir. İndirilen dosyaya çift tıklayınca internet olmadan da açılır." class="inline-flex items-center gap-1.5 text-[13px] font-semibold text-white bg-slate-800 hover:bg-slate-900 rounded-full px-4 py-2 shadow-sm transition-colors no-print">
        <i class="fa-solid fa-file-arrow-down"></i><span class="hidden sm:inline">Program Dosyasını İndir</span>
      </button>
      <button onclick="yedekAl()" title="Sistem verilerini bilgisayara yedekle (.json)" class="inline-flex items-center gap-1.5 text-[13px] font-semibold text-slate-600 hover:text-teal-600 bg-white border border-slate-200 hover:border-teal-300 rounded-full px-3.5 py-2 shadow-sm transition-colors">
        <i class="fa-solid fa-download"></i><span class="hidden sm:inline">Yedek Al</span>
      </button>
      <button onclick="document.getElementById('dosyaYukle').click()" title="Yedek dosyasından veri yükle" class="inline-flex items-center gap-1.5 text-[13px] font-semibold text-white bg-teal-500 hover:bg-teal-600 rounded-full px-3.5 py-2 shadow-sm transition-colors">
        <i class="fa-solid fa-upload"></i><span class="hidden sm:inline">Yedek Yükle</span>
      </button>
      <input type="file" id="dosyaYukle" accept="application/json,.json" class="hidden" onchange="yedekOku(this)" />
    </div>
  </div>
</header>

<!-- ============ ATLAMALI MENÜ (eklendi — mevcut yapıya dokunmaz) ============ -->
<nav id="atlamaMenu" class="no-print" aria-label="Bölümlere hızlı atla">
  <div id="atlamaMenuIcerik">
    <button type="button" class="atlama-btn" data-hedef="derslerBolum">Program</button>
    <button type="button" class="atlama-btn" data-hedef="planKart">Ders Planla</button>
    <button type="button" class="atlama-btn" data-hedef="havuzBolum">İstekler</button>
    <button type="button" class="atlama-btn" data-hedef="analizBolum">Ek Ders</button>
    <button type="button" class="atlama-btn" data-hedef="ozetBolum">Bakış</button>
    <button type="button" class="atlama-btn" data-hedef="yonetimBolum">Yönetim</button>
  </div>
</nav>

<main class="max-w-[1440px] mx-auto px-4 md:px-6 pt-6 pb-24 space-y-5">

  <!-- ============ 1) ÖZET RAPOR ============ -->
  <section id="ozetBolum" class="no-print"></section>

  <!-- ============ DETAYLI ANALİZ ============ -->
  <section id="analizBolum" class="no-print"></section>

  <!-- ============ 2) VERİ YÖNETİMİ ============ -->
  <section id="yonetimBolum" class="no-print"></section>

  <!-- ============ 3) İSTEK HAVUZU ============ -->
  <section id="havuzBolum" class="no-print"></section>

  <!-- ============ 4) BİREBİR DERS PLANLA ============ -->
  <div class="hidden" id="ks-kilavuz"><div class="no-print rounded-xl border border-teal-100 bg-teal-50/60 px-3.5 py-2.5 text-[11px] text-teal-800 leading-relaxed mb-3"><b class="font-extrabold"><i class="fa-solid fa-circle-info mr-1"></i>Ders Saati Kılavuzu:</b> Dersler 40 dakikadır ve kısa kod saatleriyle işler. 1 · 08:50-09:30 · 2 · 09:40-10:20 · 3 · 10:30-11:10 · 4 · 11:20-12:00 · 5 · 13:00-13:40 · 6 · 13:50-14:30 · 7 · 14:40-15:20 · 8 · 15:30-16:10 · 9 · 16:20-17:00 · 10 · 17:10-17:50 · 11 · 18:00-18:40. <span class="text-teal-600">Mola (12:00-13:00) ders için kullanılmaz.</span></div></div>
  <section id="planKart" class="kart p-5 md:p-6">
    <div class="flex flex-wrap items-center justify-between gap-3 mb-5">
      <div class="flex items-center gap-3">
        <span class="inline-flex items-center gap-2 bg-emerald-600 text-white text-[13px] font-bold rounded-full px-4 py-2 shadow-sm">
          <i class="fa-solid fa-plus"></i> <span id="btnBaslik">Birebir Ders Planla</span>
        </span>
        <span id="duzenleBanner" class="hidden"></span>
      </div>
      <span class="text-[11px] text-slate-400 font-medium"><i class="fa-regular fa-clock mr-1"></i>Dersler 40 dakikadır, kısa kod saatlerine göre işler</span>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-6 gap-4">
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-solid fa-user-graduate text-teal-500"></i> Öğrenci
        </label>
        <input id="f-ogrenci" list="dl-ogrenci" placeholder="Ad Soyad" autocomplete="off"
          class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400" />
        <datalist id="dl-ogrenci"></datalist>
      </div>
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-solid fa-book-open text-blue-500"></i> Ders
        </label>
        <select id="f-ders" class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400">
        </select>
      </div>
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-solid fa-file-lines text-amber-500"></i> Eksik Konu
        </label>
        <input id="f-konu" placeholder="Konu başlığı (opsiyonel)" autocomplete="off"
          class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400" />
      </div>
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-solid fa-chalkboard-user text-violet-500"></i> Öğretmen
        </label>
        <input id="f-ogretmen" list="dl-ogretmen" placeholder="Öğretmen adı" autocomplete="off"
          class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400" />
        <datalist id="dl-ogretmen"></datalist>
      </div>
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-solid fa-calendar-days text-amber-500"></i> Tarih
        </label>
        <div class="flex gap-1.5">
          <input id="f-tarih" type="date"
            class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400" />
        </div>
        <div class="flex gap-1 mt-1.5">
          <button onclick="bugunTarih()" class="text-[10px] font-semibold text-teal-600 hover:bg-teal-50 border border-teal-200 rounded-full px-2 py-0.5 transition-colors">Bugün</button>
          <button onclick="yarinTarih()" class="text-[10px] font-semibold text-teal-600 hover:bg-teal-50 border border-teal-200 rounded-full px-2 py-0.5 transition-colors">Yarın</button>
        </div>
      </div>
      <div class="lg:col-span-1">
        <label class="text-[11px] font-semibold text-slate-500 uppercase tracking-wide flex items-center gap-1.5 mb-1.5">
          <i class="fa-regular fa-clock text-cyan-500"></i> DERS SAATİ
        </label>
        <select id="f-saat"
          class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-teal-400/40 focus:border-teal-400"></select>
      </div>
    </div>

    <div class="flex flex-wrap items-center justify-between gap-3 mt-4">
      <div class="flex flex-wrap items-center gap-1.5 text-[12px] text-slate-500">
        <span class="font-semibold mr-1">Hızlı öğretmen:</span>
        <span id="hizliOgr" class="inline-flex flex-wrap gap-1.5"></span>
      </div>
      <label class="inline-flex items-center gap-2 text-[12px] text-slate-500 cursor-pointer select-none">
        <input id="f-yoksay" type="checkbox" class="w-4 h-4 rounded border-slate-300 text-teal-500 accent-teal-500" />
        <span><b class="text-slate-700">Çakışmayı Yoksay</b> / Ekstra Kontenjan</span>
      </label>
    </div>

    <div class="flex items-end justify-end mt-4 gap-2">
      <button id="btnVazgec" onclick="vazgec()"
        class="hidden rounded-full border border-slate-200 px-5 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-50 transition-colors">Vazgeç</button>
      <button onclick="planla()" id="btnPlan"
        class="inline-flex items-center gap-2 text-white text-sm font-bold rounded-full px-6 py-3 bg-gradient-to-r from-teal-500 to-emerald-500 hover:from-teal-600 hover:to-emerald-600 shadow-md shadow-teal-500/30 transition-all hover:shadow-lg hover:-translate-y-px">
        <i class="fa-solid fa-calendar-plus"></i> <span id="btnPlanYazi">Birebir Dersi Planla</span>
      </button>
    </div>

    <div id="cakismaUyari" class="hidden mt-4"></div>
  </section>

  <!-- ============ 5) DERSLER & RAPOR ============ -->
  <section id="derslerBolum"></section>

  <footer class="no-print text-center text-[11px] text-slate-400 pt-2">
    <p><i class="fa-solid fa-database mr-1"></i> Bütün öğretmen, öğrenci, istek ve ders arşivi bu bilgisayarın tarayıcısında saklanır — sayfa kapansa da silinmez. <b class="text-slate-500">Yedekleme &gt; Ayarlar</b> sekmesinden düzenli yedek alın.</p>
  </footer>
</main>

<!-- ============ ONAY MODALI ============ -->
<div id="onayModal" class="no-print hidden fixed inset-0 z-50 flex items-center justify-center p-4">
  <div class="absolute inset-0 bg-slate-900/40 backdrop-blur-[2px]" onclick="onayKapat()"></div>
  <div class="relative bg-white rounded-2xl shadow-xl w-full max-w-md p-6 belir">
    <div class="flex items-start gap-4">
      <div id="onayIkon" class="w-11 h-11 rounded-full flex items-center justify-center text-white shrink-0 bg-teal-500 text-lg"><i class="fa-solid fa-circle-question"></i></div>
      <div class="flex-1">
        <h3 id="onayBaslik" class="font-bold text-slate-900 text-[15px]"></h3>
        <p id="onayMetin" class="text-[13px] text-slate-500 mt-1.5 leading-relaxed"></p>
      </div>
    </div>
    <div class="flex justify-end gap-2 mt-6">
      <button onclick="onayKapat()" class="rounded-full px-4 py-2 text-[13px] font-semibold text-slate-600 hover:bg-slate-100 transition-colors">Vazgeç</button>
      <button id="onayButon" onclick="onayOnayla()" class="rounded-full px-5 py-2 text-[13px] font-bold text-white bg-teal-500 hover:bg-teal-600 shadow-sm transition-colors">Onayla</button>
    </div>
  </div>
</div>

<!-- ============ WHATSAPP MODALI ============ -->
<div id="waModal" class="no-print hidden fixed inset-0 z-50 flex items-center justify-center p-4">
  <div class="absolute inset-0 bg-slate-900/40 backdrop-blur-[2px]" onclick="waKapat()"></div>
  <div class="relative bg-white rounded-2xl shadow-xl w-full max-w-lg belir overflow-hidden">
    <div class="flex items-center justify-between px-5 py-4 border-b border-slate-100">
      <div class="flex items-center gap-2.5">
        <span class="w-9 h-9 rounded-full bg-green-500 text-white flex items-center justify-center"><i class="fa-brands fa-whatsapp text-lg"></i></span>
        <div>
          <h3 class="font-bold text-slate-900 text-[14px] leading-tight">WhatsApp Bilgilendirmesi</h3>
          <p id="waAlt" class="text-[11px] text-slate-400"></p>
        </div>
      </div>
      <button onclick="waKapat()" class="w-8 h-8 rounded-full hover:bg-slate-100 text-slate-400"><i class="fa-solid fa-xmark"></i></button>
    </div>
    <div id="waIcerik" class="p-3 max-h-[60vh] overflow-y-auto"></div>
  </div>
</div>

<!-- ============ PNG RAPOR MODALI ============ -->
<div id="pngModal" class="no-print hidden fixed inset-0 z-50 flex items-center justify-center p-4">
  <div class="absolute inset-0 bg-slate-900/40 backdrop-blur-[2px]" onclick="pngKapat()"></div>
  <div class="relative max-h-[92vh] overflow-y-auto rounded-2xl shadow-2xl belir">
    <div id="pngRapor"></div>
    <div class="absolute top-3 right-3 flex gap-2 no-print">
      <button onclick="pngYakala()" class="rounded-full bg-teal-500 hover:bg-teal-600 text-white text-xs font-bold px-4 py-2 shadow"><i class="fa-solid fa-download mr-1"></i>Tekrar İndir</button>
      <button onclick="pngKapat()" class="rounded-full bg-white text-slate-600 text-xs font-bold px-3 py-2 shadow"><i class="fa-solid fa-xmark"></i></button>
    </div>
  </div>
</div>

<!-- ============ BİLDİRİM ============ -->
<div id="toastAlan" class="no-print fixed bottom-5 right-5 z-[60] flex flex-col gap-2 items-end"></div>

<script src="app.js"></script>

<script>
/* ============ ATLAMALI MENÜ DAVRANIŞI (eklendi — mevcut fonksiyonlara dokunmaz) ============ */
(function () {
  var menu = document.getElementById("atlamaMenu");
  if (!menu) return;
  var butonlar = Array.prototype.slice.call(menu.querySelectorAll(".atlama-btn"));
  if (!butonlar.length) return;
  var ustBar = document.querySelector("header");

  /* Üst bar yapışkan olduğu için menü tam altına yapışsın. */
  function ustYuksekligi() {
    return (ustBar && ustBar.offsetHeight) ? ustBar.offsetHeight : 64;
  }
  function guncelleTop() {
    document.documentElement.style.setProperty("--atlamaTop", (ustYuksekligi() + 2) + "px");
  }
  guncelleTop();
  window.addEventListener("resize", guncelleTop);
  setTimeout(guncelleTop, 600);

  function vurgula(buton) {
    butonlar.forEach(function (b) {
      if (buton && b === buton) {
        b.classList.add("aktif");
        b.setAttribute("aria-current", "true");
      } else {
        b.classList.remove("aktif");
        b.removeAttribute("aria-current");
      }
    });
  }

  /* Tıklayınca ilgili bölüme yumuşak kaydır. */
  butonlar.forEach(function (buton) {
    buton.addEventListener("click", function () {
      var hedef = document.getElementById(buton.getAttribute("data-hedef"));
      if (!hedef) return;
      vurgula(buton);
      hedef.scrollIntoView({ behavior: "smooth", block: "start" });
    });
  });

  /* Kaydırma sırasında hangi bölümdeysek o buton vurgulu olsun. */
  var bekleyen = false;
  function spy() {
    if (bekleyen) return;
    bekleyen = true;
    requestAnimationFrame(function () {
      bekleyen = false;
      var esik = ustYuksekligi() + menu.offsetHeight + 10;
      var aktif = null;
      for (var i = 0; i < butonlar.length; i++) {
        var hedef = document.getElementById(butonlar[i].getAttribute("data-hedef"));
        if (hedef && hedef.getBoundingClientRect().top <= esik) aktif = butonlar[i];
      }
      /* Sayfa en başındayken ilk bölüm (Bakış) vurgulu görünsün. */
      var kayma = window.scrollY !== undefined ? window.scrollY : (document.documentElement && document.documentElement.scrollTop) || 0;
      if (!aktif && kayma < 120) aktif = butonlar[0];
      /* Sayfa sonuna gelindiyse son bölüm vurgulu olsun. */
      if (window.innerHeight + window.scrollY >= document.documentElement.scrollHeight - 4) {
        aktif = butonlar[butonlar.length - 1];
      }
      vurgula(aktif);
    });
  }
  window.addEventListener("scroll", spy, { passive: true });
  spy();
})();
</script>
</body>
</html>
